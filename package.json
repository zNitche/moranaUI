{
    "name": "moranaui_workspace",
    "private": true,
    "type": "module",
    "author": "zNitche",
    "workspaces": [
        "lib",
        "example_app"
    ],
    "scripts": {
        "test:lib": "npm run test --workspace=lib",
        "lint:lib": "npm run lint --workspace=lib",
        "tsc:lib": "npm run tsc --workspace=lib",
        "build:lib": "npm run build --workspace=lib",
        "build:resolve-imports-aliases": "npm run build:resolve-imports-aliases --workspace=lib",
        "tsc:app": "npm run tsc --workspace=example_app",
        "lint:app": "npm run lint --workspace=example_app"
    }
}